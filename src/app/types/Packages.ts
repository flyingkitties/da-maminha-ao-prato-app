import { HTMLAttributes } from 'react';

export interface Packages {
  id: number;
  title: string;
  prevPrice: string;
  price: string;
  list: string[];
  color: string;
  borderColor: string;
  textColor: string;
}
